% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iterate_Lease_Template_GT.R
\name{iterate_Lease_Template_GT}
\alias{iterate_Lease_Template_GT}
\title{Iterate Lease Template with PNG GT Tables}
\usage{
iterate_Lease_Template_GT(output_dir = "output")
}
\arguments{
\item{output_dir}{The directory where the output DOCX files will be saved.
This path should be writable and will contain the rendered reports.
If the directory does not exist, it will be created.}
}
\value{
Invisible \code{NULL}. The function is called for its side effects
(rendering and saving documents) and does not return a value.
}
\description{
This function iterates over \code{Lease_Template_GT.qmd} with PNG GT Tables,
rendering a Word document for each lease entry in the provided dataset.
The rendered documents are saved to the specified output directory.
}
\examples{
# Assuming `gtlsLsePkg` is installed and `leases2merge.xlsx` is available
# in the package's 'inst/data' directory:
iterate_Lease_Template_GT(output_dir = "path/to/save/reports")

}
